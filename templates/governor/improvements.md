# Governor improvements

The automated self-improvement lane (`govern-improve.sh` observe → propose, `govern-self-apply.sh`
guarded auto-apply) was retired along with the run-level driver that fired it. This file is now
operator-maintained notes on harness friction: a concrete, scoped change worth making to the
harness, written here by hand and applied (or filed as a ticket) at your own pace. Safety rails are
never auto-changed; if one caused friction it's flagged `OPERATOR DECISION`.

_(empty, add a note here when you notice recurring friction)_
