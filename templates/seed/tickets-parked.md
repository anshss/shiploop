# Tickets — parked

Deferred work items. The **governor ignores this file** — move a ticket here to take it out of the
autonomous queue without losing it. This is its own independent serial `## #N` list starting at #1
(it does NOT share numbering with `tickets.md`). When you move a ticket between the two files,
**renumber it to the destination queue's max + 1** — it does not keep its old number.

---

_(empty)_
